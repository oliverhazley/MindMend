/*
  info.js
  - Loaded by "info.html".
  - If you have dynamic content (like toggling sections, loading external resources),
    place that logic here. Otherwise, you may leave it mostly empty.
*/
document.addEventListener("DOMContentLoaded", () => {
  console.log("info.js loaded. Info page logic can go here if needed.");
  // e.g., load additional data from an API, toggle FAQ sections, etc.
});
